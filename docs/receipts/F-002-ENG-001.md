# F-002-ENG-001 - Read-only Receipt

**Feature:** F-002 Farmboard Visual Language  
**Status:** READY  
**Prepared by:** Codex /root, current producer session  
**Owner review:** Codex /root, accepted for the explicitly scoped implementation below

## Loaded context

- CityOfAnimals project adapter: completed against workflow, profile, active scope, handoff, current task contract, design index, config index, and feature matrix.
- Primary workflow: `game-studio-orchestrator`.
- Primary implementation Skill: `godot-feature-slice-implementation`.
- UI hierarchy Skill: `ui-design-priority`.
- Research route: Agent Reach. Doctor succeeded through the local proxy; its available web backend is Jina Reader. Public visual review was used only to infer generic object-first farm readability. No external pixels, text, or assets are used.

## Source and baseline verification

- F-001 is complete, no feature is active, no shared lock exists, and the user's F-002 direction is the sole next P0 request.
- F-002 functional source and F-002 language configuration now define the behavior without changing F-001 numeric content.
- All contract baseline hashes match the files observed before implementation. The main scene and project settings are explicitly forbidden.
- The stable Godot 4.6.2 console executable exists. No Godot process was active in the implementation preflight snapshot.

## Planned write set

- `scripts/town/town_model.gd`: replace presentation strings with feedback keys and values only; preserve state transitions and F-001 numbers.
- `scripts/town/town_text.gd`: load the F-002 language CSV and persist `zh-CN` or `en` preference.
- `scripts/town/town_view.gd`: replace text-card rendering with original vector Farmboard objects, direct-object hit regions, compact settings tray, and localized short feedback.
- `tests/test_town_model.gd`: preserve loop regression coverage; add visual hit, catalog, and persisted-locale assertions.
- `config/tables/f002_town_ui.csv`: correct only discovered catalog defects.

## Concerns and non-goals

- The first F-002 runtime run must use isolated `APPDATA` and `LOCALAPPDATA`, because a prior project import encountered a one-off headless driver crash.
- The language setting is intentionally a small tray rather than a new menu system.
- No agent, sub-agent, or external execution task is authorized or required.

## Decision

**READY.** The accountable owner accepts this receipt and authorizes only the contract's isolated write scope. Stop if a baseline changes, a second writer appears, the F-001 numeric CSV changes, or the visual redesign requires a new product system.
