# ART-002 Blocker Receipt

**状态：** BLOCKED  
**阻塞项：** 内置图像生成服务网络错误  

P01–P03 已在默认生成目录产出预览；P04 在两次内置工具尝试中均因网络错误失败。未复制任何分区板到项目，因此候选目录内没有不完整的正式或可误用的建筑资源。

可继续路径：制作人明确授权后使用需要本机 `OPENAI_API_KEY` 的 CLI 备用生成路径，或等待并重试内置生成服务。无论哪条路径，都必须继续使用 ART-002 的完整清单、尺寸阶梯、ART-001 风格来源与 `NOT_RUNTIME` 边界。
